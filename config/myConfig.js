import { useTranslations } from "next-intl"

// Json Imports
import dataEarn from '../data/earn.json'
import dataCards from '../data/cards.json'
import dataCarousel from '../data/carousel.json'
import dataDeploy from '../data/deploy.json'
import dataEasy from '../data/easy.json'
import dataFooter from '../data/footer.json'
import dataHero from '../data/hero.json'
import dataMultiple from '../data/hero.json'
import dataNft from '../data/nft.json'
import dataProducts from '../data/products.json'

export const headerConfig = {
  
}
export const earnConfig = {
  data: dataEarn
}
export const cardsConfig = {
  data:dataCards
}
export const carouselConfig = {
  data:dataCarousel
}
export const deployConfig = {
  data:dataDeploy
}
export const easyConfig = {
  data:dataEasy
}
export const footerConfig = {
  data:dataFooter
}
export const heroConfig = {
  data:dataHero
}
export const multipleConfig = {
  data: dataMultiple
}
export const nftConfig = {
  data:dataNft
}
export const productsConfig = {
  data:dataProducts
}